  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :name %>
    <%= f.input :address %>
    <%= f.input :phone_number %>
    <%= f.input :category, collection: Restaurant::CATEGORIES %>
    <div class="mt-3"><%= f.submit %></div>
  <% end %>
